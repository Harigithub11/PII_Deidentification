version: '3.8'

services:
<<<<<<< HEAD
  # PostgreSQL Database
  database:
    image: postgres:15-alpine
    container_name: deidentify-db
    environment:
      POSTGRES_DB: deidentify_db
      POSTGRES_USER: deidentify_user
      POSTGRES_PASSWORD: secure_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    networks:
      - deidentify-network
    restart: unless-stopped

  # Redis for job queue
  redis:
    image: redis:7-alpine
    container_name: deidentify-redis
    ports:
      - "6379:6379"
    networks:
      - deidentify-network
    restart: unless-stopped

  # Main Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: deidentify-app
    environment:
      - DATABASE_URL=postgresql://deidentify_user:secure_password@database:5432/deidentify_db
      - REDIS_URL=redis://redis:6379/0
=======
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
>>>>>>> c8ebce61f799d32c82776b671db0133da2fce30e
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
<<<<<<< HEAD
      - ./logs:/app/logs
      - ./config:/app/config
    depends_on:
      - database
      - redis
    networks:
      - deidentify-network
    restart: unless-stopped

  # Prefect Server (Workflow Management)
  prefect-server:
    image: prefecthq/prefect:2-python3.10
    container_name: deidentify-prefect
    environment:
      - PREFECT_SERVER_API_HOST=0.0.0.0
      - PREFECT_API_DATABASE_CONNECTION_URL=postgresql+asyncpg://deidentify_user:secure_password@database:5432/deidentify_db
    ports:
      - "4200:4200"
    command: prefect server start --host 0.0.0.0
    depends_on:
      - database
    networks:
      - deidentify-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  deidentify-network:
    driver: bridge
=======
      - ./models:/app/models
      - ./logs:/app/logs
      - ./config:/app/config
    environment:
      - DATABASE_URL=sqlite:///./data/pii_system.db
      - MODEL_CACHE_DIR=/app/models/cache
      - ENABLE_GPU=false
    depends_on:
      - airflow-webserver
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: ../docker/Dockerfile.frontend
    ports:
      - "3000:3000"
    depends_on:
      - api
    restart: unless-stopped
    environment:
      - REACT_APP_API_URL=http://localhost:8000

  airflow-webserver:
    build:
      context: .
      dockerfile: docker/Dockerfile.worker
    command: airflow webserver
    ports:
      - "8080:8080"
    volumes:
      - ./orchestration:/app/orchestration
      - ./data:/app/data
      - ./logs/airflow:/app/logs
      - ./config:/app/config
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=sqlite:///./data/airflow.db
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__FERNET_KEY=your-fernet-key-here
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  airflow-scheduler:
    build:
      context: .
      dockerfile: docker/Dockerfile.worker
    command: airflow scheduler
    volumes:
      - ./orchestration:/app/orchestration
      - ./data:/app/data
      - ./logs/airflow:/app/logs
      - ./config:/app/config
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=sqlite:///./data/airflow.db
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__FERNET_KEY=your-fernet-key-here
    depends_on:
      - airflow-webserver
    restart: unless-stopped

  # Optional: PostgreSQL for production use
  # postgres:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: pii_system
  #     POSTGRES_USER: pii_user
  #     POSTGRES_PASSWORD: pii_password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

  # Optional: Redis for Celery backend
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   restart: unless-stopped

volumes:
  data:
  models:
  logs:
  # postgres_data:  # Uncomment for PostgreSQL
>>>>>>> c8ebce61f799d32c82776b671db0133da2fce30e
